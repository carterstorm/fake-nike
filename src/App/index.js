import { HashRouter } from "react-router-dom";
import { HeaderElement } from "../features/HeaderElement";

const App = () => (
  <HashRouter>
    <HeaderElement />
  </HashRouter>
);

export default App;
