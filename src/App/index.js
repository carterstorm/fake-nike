import { HashRouter } from "react-router-dom";
import { HeaderElement } from "../common/HeaderElement";

const App = () => (
  <HashRouter>
    <HeaderElement />
  </HashRouter>
);

export default App;
