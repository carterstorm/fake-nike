import { HashRouter } from "react-router-dom";
import { NavigationElement } from "../common/NavigationElement";

const App = () => (
  <HashRouter>
    <NavigationElement />
  </HashRouter>
);

export default App;
