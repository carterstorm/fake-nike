import { HashRouter } from "react-router-dom";
import { Navigation } from "../common/Navigation";

const App = () => (
  <HashRouter>
    <Navigation>

    </Navigation>
  </HashRouter>
);

export default App;
